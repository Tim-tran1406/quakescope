# Copy this file to ".env" and fill in your real values.
# The real ".env" is gitignored so your password never goes to GitHub.

# PostgreSQL connection — we'll fill these in once Postgres is installed (Phase 0/2).
DB_HOST=localhost
DB_PORT=5432
DB_NAME=quakescope
DB_USER=tim
DB_PASSWORD=
